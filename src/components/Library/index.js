import React from 'react';
import { SectionWrapper } from 'globalStyles';

const Library = ({ token }) => {
  return (
    <SectionWrapper>
      <h1>Library</h1>
    </SectionWrapper>
  );
};

export default Library;